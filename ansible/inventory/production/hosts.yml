all:
  children:

    database:
      hosts:
        db_server:
          ansible_host: 10.10.30.2
          ansible_user: db_server
          ansible_port: 22
          ansible_ssh_private_key_file: ~/.ssh/ansible_ensync
