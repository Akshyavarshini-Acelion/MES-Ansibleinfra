---
# Telemetry service ports

mqtt_port: 9000
mqtt_fallback_port: 9001
socket_tunnel_port: 5000
fins_port: 9600
gvcp_port: 3956

# IMPORTANT:
# MQTT and MES Backend API cannot both bind to port 9000
# on the same VM/IP. Set the final MES backend port here
# after the service port is confirmed.
mes_backend_port: 8000
